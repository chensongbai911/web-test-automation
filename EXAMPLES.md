# Web 功能自动化测试工具 - 完整使用示例

## 目录

1. [基础操作](#基础操作)
2. [实际案例](#实际案例)
3. [报告解读](#报告解读)
4. [故障排除](#故障排除)
5. [高级用法](#高级用法)

---

## 基础操作

### 安装和加载插件

#### 步骤 1：解压插件文件

```
web-test-automation/
├── manifest.json
├── src/
│   ├── popup.html
│   ├── popup.css
│   ├── popup.js
│   ├── content-script.js
│   ├── background.js
│   ├── report.html
│   ├── report.css
│   └── report.js
└── README.md
```

#### 步骤 2：在 Chrome 中加载

1. 打开 Chrome，进入 `chrome://extensions/`
2. 启用"开发者模式"（右上角开关）
3. 点击"加载已解压的扩展程序"
4. 选择 `web-test-automation` 文件夹
5. 确认加载成功

#### 步骤 3：验证安装

- Chrome 右上角出现插件图标
- 点击图标，弹窗显示正常

---

## 实际案例

### 案例 1：测试电商网站首页

#### 目标

测试电商网站的首页，检查所有功能按钮是否正常

#### 步骤

1. **打开插件弹窗**

   ```
   点击Chrome右上角的插件图标
   ```

2. **输入网址**

   ```
   URL输入框：https://www.example-shop.com
   ```

3. **配置选项**

   ```
   ✓ 测试交互按钮 (已选中)
   ✓ 监控API请求 (已选中)
   ☐ 捕获截图 (可选)
   ✓ 捕获控制台日志 (已选中)
   ```

4. **启动测试**

   ```
   点击"开始测试"按钮
   ```

5. **等待测试完成**

   ```
   实时监控：
   - 已测试按钮: 0/32
   - 成功: 0
   - 失败: 0
   - API错误: 0

   进度条显示：█████░░░░░░░░░░░░░░ 25%

   日志输出：
   [10:30:15] 页面加载完成，开始获取可交互元素...
   [10:30:16] 找到 32 个可交互元素，开始测试...
   [10:30:17] [1/32] 测试 button: 搜索
   [10:30:17] API请求: GET https://api.example.com/search - 200
   [10:30:17] ✓ 测试成功: 搜索
   ```

6. **查看报告**
   ```
   点击"查看报告"按钮
   新标签页打开完整的可视化报告
   ```

#### 预期结果

报告显示：

- 测试元素总数: 32
- 成功: 30 (93.75%)
- 失败: 2 (6.25%)
- API 错误: 1

**饼图**: 绿色 93%（成功），红色 7%（失败）

**柱状图**:

- 按钮: 18 个
- 链接: 10 个
- 输入框: 4 个

**API 分析**:

- 总请求: 28 个
- 2xx (成功): 27 个
- 4xx (客户端错误): 1 个
- 5xx (服务器错误): 0 个

#### 问题定位

假如失败的 2 个按钮是：

1. "立即购买"按钮 - API 返回 500 错误
2. "分享到微信"按钮 - 链接 404

**对应行动**:

- 通知后端修复购买接口
- 检查分享页面 URL 是否正确

---

### 案例 2：测试登录表单

#### 目标

验证登录表单的所有字段和按钮

#### 步骤

1. **输入登录页 URL**

   ```
   https://www.example.com/login
   ```

2. **启动测试**

   ```
   点击"开始测试"
   ```

3. **查看测试过程**

   插件会自动：

   - 识别用户名输入框
   - 识别密码输入框
   - 识别"记住我"复选框
   - 识别"登录"按钮
   - 识别"忘记密码"链接
   - 依次输入测试数据和点击

4. **分析报告**

   | 元素     | 类型     | 状态 | 备注                   |
   | -------- | -------- | ---- | ---------------------- |
   | 用户名   | input    | ✓    | 成功输入               |
   | 密码     | input    | ✓    | 成功输入               |
   | 记住我   | checkbox | ✓    | 成功点击               |
   | 登录按钮 | button   | ✓    | 成功点击，API 返回 200 |
   | 忘记密码 | link     | ✓    | 成功打开新页面         |

#### 可能的问题

如果发现"登录"按钮失败：

```
原因可能：
1. API返回非200状态码（如401、500）
2. 表单验证失败
3. 登录接口超时

解决方向：
1. 检查登录API是否正常
2. 检查表单验证规则
3. 检查服务器负载
```

---

### 案例 3：对比两个版本网站

#### 目标

对比旧版和新版网站的测试结果

#### 步骤

1. **测试旧版本**

   ```
   URL: https://old.example.com
   点击"开始测试"
   等待完成
   点击"查看报告" → 导出JSON
   保存为: old-version-report.json
   ```

2. **测试新版本**

   ```
   URL: https://new.example.com
   点击"开始测试"
   等待完成
   点击"查看报告" → 导出JSON
   保存为: new-version-report.json
   ```

3. **对比分析**

   在 Excel 或文本编辑器中对比：

   | 指标     | 旧版 | 新版 | 变化 |
   | -------- | ---- | ---- | ---- |
   | 总元素   | 45   | 52   | +7   |
   | 成功率   | 95%  | 98%  | +3%  |
   | API 错误 | 2    | 1    | -1   |

#### 结论

新版本：

- ✅ 添加了 7 个新功能元素
- ✅ 成功率提升了 3%
- ✅ 减少了 API 错误

---

## 报告解读

### 统计概览卡片

```
┌─────────────────────────────────────┐
│ 📋 测试元素总数                    │
│        45                            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ✓ 测试成功                         │
│        40                            │
│    (88.9%)                           │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ✗ 测试失败                         │
│        3                             │
│    (6.7%)                            │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ ⚠ API错误                          │
│        2                             │
│    (4.4%)                            │
└─────────────────────────────────────┘
```

**解读**:

- 成功率 88.9%是可以接受的
- 需要关注 3 个失败的元素
- 2 个 API 错误需要后端检查

### 饼图

```
        成功: 40 (88.9%)
        ┌─────╨────────┐
        │      ▓▓▓▓▓▓▓  │
        │   ▓▓▓       ▓ │
        │ ▓▓             ▓
        │▓    失败: 3    ▓
        │▓   (6.7%)   ▓
        │ ▓▓         ▓  │
        │   ▓▓▓   ▓▓    │
        │      ▓▓▓      │
        └────────────────┘

        绿色（成功）占比88.9%
        红色（失败）占比6.7%
```

**解读**:

- 绿色越多越好
- 失败比例< 10%通常可接受
- 如果失败> 20%，应深入调查

### 柱状图

```
元素类型统计

数量 │
     │
 20  │  ┌─────┐
 18  │  │     │ ┌─────┐
 16  │  │     │ │     │
 14  │  │     │ │     │
 12  │  │     │ │     │ ┌──┐
 10  │  │     │ │     │ │  │
  8  │  │     │ │     │ │  │
  6  │  │     │ │     │ │  │ ┌┐
  4  │  │     │ │     │ │  │ ││
  2  │  │     │ │     │ │  │ ││
  0  └──┴─────┴─┴─────┴─┴──┴─┴┘
     button  link  input select
       18      15     10    2
```

**解读**:

- 按钮占比最多（18 个）
- 链接次之（15 个）
- 输入框较少（10 个）
- 下拉框很少（2 个）

这反映了网站的主要交互方式。

### API 分析

```
HTTP状态码分类

2xx (200-299) 成功      : 25个 ✓
3xx (300-399) 重定向    : 2个 ℹ
4xx (400-499) 客户端错误: 1个 ⚠
5xx (500-599) 服务器错误: 1个 ✗
```

**问题定位**:

1. **1 个 4xx 错误**: 可能是请求参数错误或资源不存在
2. **1 个 5xx 错误**: 服务器问题，需要后端检查日志

### 详细表格

```
序号 | 类型   | 文本       | 状态
-----|--------|-----------|------
  1  | button | 搜索      | ✓
  2  | button | 登录      | ✓
  3  | button | 注册      | ✓
  4  | link   | 首页      | ✓
  5  | link   | 商品      | ✗
  6  | input  | 搜索框    | ✓
```

**问题定位**:

- 第 5 行的"商品"链接失败
- 可能原因：链接地址错误、页面已删除、权限不足

---

## 故障排除

### 问题 1：无法连接到页面

**错误信息**

```
[ERROR] 无法连接到页面，请确保页面已加载
```

**可能原因**

1. URL 格式不正确
2. 网络连接问题
3. 页面加载超时

**解决方法**

```
1. 检查URL格式是否包含 http:// 或 https://
2. 手动访问该URL验证是否可达
3. 等待更长时间再尝试
4. 检查是否需要代理或VPN
```

### 问题 2：大量测试失败

**现象**

```
成功: 2/45
失败: 43/45
```

**可能原因**

1. 网站被保护（需要登录）
2. 网站使用了动态加载（JavaScript 框架）
3. 网站有反爬虫机制

**解决方法**

```
1. 测试未登录的公开页面
2. 等待JavaScript完全加载后再测试
3. 检查网站的robots.txt或条款
```

### 问题 3：报告页面显示不正确

**现象**

```
报告页面白屏或数据不显示
```

**可能原因**

1. 测试数据未保存
2. 浏览器存储满了
3. JavaScript 错误

**解决方法**

```
1. 在F12控制台检查错误信息
2. 清除浏览器缓存: Ctrl+Shift+Delete
3. 重新运行测试生成新数据
4. 尝试在隐身窗口运行
```

### 问题 4：API 错误过多

**现象**

```
API错误: 10/28
```

**可能原因**

1. API 服务器出问题
2. 频率限制被触发
3. 请求头缺少必要字段

**解决方法**

```
1. 在浏览器Network标签检查单个请求
2. 查看API返回的具体错误信息
3. 检查网站是否有速率限制
4. 咨询网站技术支持
```

---

## 高级用法

### 自定义测试参数

#### 修改测试间隔

编辑 `src/content-script.js`：

```javascript
// 修改元素间隔时间（毫秒）
await delay(800);  // 改为其他值，如1000

// 修改页面加载等待时间
setTimeout(() => { ... }, 2000);  // 改为更长的时间如5000
```

#### 修改元素识别规则

编辑 `src/content-script.js`：

```javascript
function getInteractiveElements() {
  const elements = [];

  // 添加自定义选择器
  document.querySelectorAll("button.custom-btn").forEach((el) => {
    elements.push({
      element: el,
      type: "custom-button",
      text: el.textContent.trim(),
      selector: getElementSelector(el),
    });
  });

  return elements;
}
```

### 整合 CI/CD 流程

#### 与 GitHub Actions 集成

创建 `.github/workflows/test.yml`：

```yaml
name: Web Auto Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Chrome Extension
        run: |
          # 加载插件
          chrome --load-extension=./web-test-automation
      - name: Run Tests
        run: |
          # 运行测试脚本
          node scripts/run-tests.js
      - name: Generate Report
        run: |
          # 生成报告
          node scripts/generate-report.js
      - name: Upload Report
        uses: actions/upload-artifact@v2
        with:
          name: test-report
          path: reports/
```

### 自动化多网站测试

创建 `scripts/run-tests.js`：

```javascript
const websites = [
  "https://www.example1.com",
  "https://www.example2.com",
  "https://www.example3.com",
];

async function testAll() {
  const results = [];

  for (const url of websites) {
    console.log(`Testing: ${url}`);
    const result = await testWebsite(url);
    results.push(result);
  }

  // 生成对比报告
  generateComparisonReport(results);
}

testAll();
```

### 性能监控增强

编辑 `src/content-script.js` 添加性能指标：

```javascript
// 收集性能指标
const perfMetrics = {
  navigationStart: performance.timing.navigationStart,
  loadComplete: performance.timing.loadEventEnd,
  domReady: performance.timing.domContentLoadedEventEnd,
  firstPaint: performance.timing.responseEnd,
  pageLoadTime:
    performance.timing.loadEventEnd - performance.timing.navigationStart,
};

// 添加到测试报告
testData.performance = perfMetrics;
```

### 使用 Selenium 自动化扩展

创建 `scripts/selenium-extension.py`：

```python
from selenium import webdriver
from selenium.webdriver.chrome.options import Options

chrome_options = Options()
# 添加插件路径
chrome_options.add_extension('./web-test-automation')

driver = webdriver.Chrome(options=chrome_options)
driver.get('https://example.com')

# 触发插件测试
driver.execute_script("""
  chrome.runtime.sendMessage({
    action: 'startTest',
    config: {...}
  });
""")
```

---

## 最佳实践

### ✅ 推荐做法

1. **定期测试**

   - 每周运行一次完整测试
   - 每次发布前运行回归测试
   - 监控测试趋势

2. **数据保存**

   - 导出 JSON 存档
   - 建立历史对比
   - 追踪问题修复进度

3. **团队协作**

   - 生成报告分享给团队
   - 建立问题优先级
   - 跟踪修复状态

4. **持续改进**
   - 根据测试结果优化代码
   - 扩展测试覆盖范围
   - 收集用户反馈

### ❌ 应避免

1. 在生产网站上进行可能修改数据的测试
2. 频繁请求可能触发速率限制
3. 忽视 API 错误，假设网站有问题
4. 不备份重要的测试报告

---

## 更多资源

- [Chrome 扩展开发文档](https://developer.chrome.com/docs/extensions/)
- [Manifest V3 指南](https://developer.chrome.com/docs/extensions/mv3/)
- [Chart.js 文档](https://www.chartjs.org/)
- [Web API 参考](https://developer.mozilla.org/zh-CN/docs/Web/API)

---

**版本**: 1.0.0
**最后更新**: 2024 年 1 月
